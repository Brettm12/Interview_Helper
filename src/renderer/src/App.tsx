// Placeholder shell — replaced during integration with the window router
// (live panel / strip / bank / setup / recap) + demo gallery.
export default function App() {
  return (
    <div style={{ padding: 24 }}>
      <div className="label">LIVE INTERVIEW HELPER · SCAFFOLD</div>
    </div>
  )
}
